version: '3.1'
services:
  redmine:
    image: redmine:{{redmine_image_version}}
    restart: always
    ports:
      - "{{redmine_port}}:3000"
  db:
    image: mysql:{{mysql_image_version}}
    restart: always